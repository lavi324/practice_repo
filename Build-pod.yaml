apiVersion: v1
kind: Pod
metadata:
  name: docker-image-build
  namespace: jenkins
spec:
  containers:  
  - name: dind
    image: docker:dind
    imagePullPolicy: Always
    securityContext:
      privileged: true